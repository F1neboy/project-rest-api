package com.project.rest;
/*
import com.project.rest.auth.AuthenticationRequest;
import com.project.rest.auth.AuthenticationResponse;
import com.project.rest.auth.ReqisterRequest;
import com.project.rest.controller.AuthController;
import com.project.rest.controller.Impl.AuthControllerImpl;
import com.project.rest.model.User;
import org.junit.jupiter.api.Test;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

class AuthControllerTest {

    private final AuthController authController = new AuthControllerImpl();

    @Test
    void testRegister() {
        // Arrange
        ReqisterRequest request = new ReqisterRequest();

        // Act
        ResponseEntity<AuthenticationResponse> result = authController.register(request);

        // Assert
        assertEquals(HttpStatus.OK, result.getStatusCode());
        assertNotNull(result.getBody());
        // Dodaj dalsze asercje w zależności od oczekiwanego wyniku
    }

    @Test
    void testLogin() {
        // Arrange
        AuthenticationRequest request = new AuthenticationRequest();

        // Act
        ResponseEntity<AuthenticationResponse> result = authController.login(request);

        // Assert
        assertEquals(HttpStatus.OK, result.getStatusCode());
        assertNotNull(result.getBody());
        // Dodaj dalsze asercje w zależności od oczekiwanego wyniku
    }

    @Test
    void testTest1() {
        // Act
        String result = authController.test1();

        // Assert
        assertNotNull(result);
        // Dodaj dalsze asercje w zależności od oczekiwanego wyniku
    }

    @Test
    void testAll() {
        // Act
        List<User> result = authController.all();

        // Assert
        assertNotNull(result);
        // Dodaj dalsze asercje w zależności od oczekiwanego wyniku
    }

}

*/